// Keynote renders as a single full-screen deck (see SiteWatermark.tsx, which
// owns the "/" route). The stacked Index sections render nothing here.
export default function Hero() {
  return null;
}
