// Keynote = full-bleed opaque slides; presenter chrome lives in the header and
// footer, so a background watermark would never show through. Intentionally empty.
export default function SiteWatermark() {
  return null;
}
